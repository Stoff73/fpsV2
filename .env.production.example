# ==================================================
# TenGo v0.1.2.13 - Production Environment Configuration
# ==================================================
# For deployment to: csjones.co/tengo
# ==================================================

# Application Settings
APP_NAME="TenGo - Financial Planning System"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=false
APP_URL=https://csjones.co/tengo
APP_TIMEZONE=Europe/London
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_GB

# Database Configuration
# IMPORTANT: Use your SiteGround database credentials
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=dbow3dj6o4qnc4
DB_USERNAME=uixybijdvk3yv
DB_PASSWORD=YOUR_DATABASE_PASSWORD

# Cache & Session Configuration
# Use 'file' driver for SiteGround shared hosting
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/tengo
SESSION_DOMAIN=csjones.co
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Queue Configuration
# Use 'database' for background jobs (Monte Carlo simulations)
QUEUE_CONNECTION=database

# Logging
LOG_CHANNEL=daily
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning
LOG_DAILY_DAYS=14

# Mail Configuration
# IMPORTANT: Configure with your SiteGround email settings
MAIL_MAILER=smtp
MAIL_HOST=mail.csjones.co
MAIL_PORT=587
MAIL_USERNAME=noreply@csjones.co
MAIL_PASSWORD=YOUR_EMAIL_PASSWORD
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@csjones.co
MAIL_FROM_NAME="${APP_NAME}"

# Sanctum (API Authentication)
SANCTUM_STATEFUL_DOMAINS=csjones.co
SANCTUM_TOKEN_PREFIX=

# Security Settings
BCRYPT_ROUNDS=12

# Asset Configuration
ASSET_URL=/tengo

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"

# Timezone
TZ=Europe/London
APP_TIMEZONE=Europe/London

# Sanctum Token Expiration (in minutes)
SANCTUM_TOKEN_EXPIRATION=480

# ==================================================
# DEPLOYMENT CHECKLIST
# ==================================================
#
# Before deploying, ensure you:
#
# 1. Generate APP_KEY:
#    php artisan key:generate --show
#    Copy output to APP_KEY above
#
# 2. Update all YOUR_* placeholders with actual values
#
# 3. Set correct database credentials from SiteGround
#
# 4. Configure email with SiteGround email account
#
# 5. Set APP_DEBUG=false for production
#
# 6. Use HTTPS (APP_URL starts with https://)
#
# 7. Set SESSION_SECURE_COOKIE=true for HTTPS
#
# ==================================================
